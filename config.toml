[service]
name = "zambian-road-traffic-ai"

[deployment]
provider = "streamlit"
mainFile = "app_basic.py"

# Fallback options if basic app fails:
# Option 1: streamlit_app.py (full RAG functionality)
# Option 2: app.py (original full version)

[environment]
required = ["GROQ_API_KEY"]
